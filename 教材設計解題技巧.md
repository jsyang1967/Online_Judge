---
title: 教材設計 解題技巧
tags: Online Judge, ICPC
description: 線上評分系統, ICPC, Program, Online
---

## 教材設計 解題技巧

### 十進位的數字轉換成二進位
:::info
一個十進位的數字轉換成二進位的數字。
:::

#### Input
輸入若干行直到 EOF 為止。每一行包含一個十進位的整數
```=
3
6
```
#### Output
針對每一行的整數輸出其二進位制的結果
```=
11
110
```

#### Answer
```python=
#include<stdio.h>

void show_binary(int k) {
    int ans[100];
    int index = 0;
    int i = 0;

    while (k != 0) {
        ans[i] = (k % 2);
        k /= 2;
        index++;
        i++;
    }

    for (int j = index-1; j >=0; j--)  printf("%d", ans[j]);

}

int main(void)
{
    int num;
    while (scanf("%d", &num) != EOF)     {
        show_binary(num);
        printf("\n");
    }
    return 0;
}
```

---


### 計算位元為 1 的個數
:::info
一個十進位的數字轉換成二進位的數字，計算位元為 1 的個數。
https://zerojudge.ntub.tw/jury/problems/16
:::


#### Input
```=
~~4~~
0
16
1025
65535
```
#### Output
```=
0
1
2
16
```

#### Answer
```python=
import sys
n = int(sys.stdin.readline())

def solve():
    m = int(sys.stdin.readline())
    re = bin(m).replace("0b","").replace("0","")
    print(len(re))

for i in range(n):
    solve()

```

---


### 出題模板
:::info
一個$d$位數整數$N=n_1n_2n_3 \dots n_d$，$n_i \in  \{1,2,3,4,5,6,7,8,9,0 \}$，被稱為自戀數（narcissistic number）若$N=n_1^d+n_2^d+n_3^d+ \dots+ n_d^d$。例如3位數整數153是自戀數因為$153=1^3+5^3+3^3$，而4位數整數1321不是自戀數因為$1321=1^4+3^4+2^4+1^4=99$非1321。
非十進位制數字數字也會有自戀數，例如3進位制數字$(122)_3=17$且$122=1^3+2^3+2^3=17$，或5進位制數字$(3134)_5=419$且$3134=3^4+1^4+3^4+4^4=419$。
請寫一個程式判斷一整數是否為自戀數。
:::
#### Input
```=
第一列有兩個非負整數$b$與$N$，代表$N$為$b$進位制整數。
```
```
Sample Input #1
10 153

Sample Input #2
10 1321

Sample Input #3
3 122

Sample Input #4
5 3134
```
#### Output
```=
若輸出之整數為自戀數，輸出YES，若否則輸出NO。
```
```
Sample Output #1
YES

Sample Output #2
NO

Sample Output #3
YES

Sample Output #4
YES
```
https://tioj.ck.tp.edu.tw/problems/2035

#### Answer

```c=
#include <iostream>
#include <cmath>
using namespace std;
 
int main()
{
    string s;
    int b, l, i;
    while (cin >> b >> s) {
        l = s.size();
        int num1 = 0, num2 = 0;
        for (i = 0; i < l; i++) {
            num1 += (s[i] - '0') * pow(b, (l - 1 - i));
            num2 += pow((s[i] - '0'), l);
        }
        if (num1 == num2) {
            printf("YES\n");
        }
        else {
            printf("NO\n");
        }
    }
}

```
https://yuihuang.com/zj-c459/
---



### 四則運算
:::info
題意是輸入一個字串譬如2 * ( 3 + 4 ) * 5，
根據先乘除後加減及() 優先的運算規則，
把字串代表的值算出來(譬如說2 * ( 3 + 4 ) * 5 = 70)
(字串可能包含%(取餘數)，取餘數的運算優先順序與乘除同等級)
首先讀到一個字串，先把它轉換從infix表達式轉換成postfix，
再透過之前計算postfix表達式的算法，即可解開。
資結經典題目，
:::
https://ithelp.ithome.com.tw/articles/10231419
類似題目
leetcode- 224. Basic Calculator
leetcode- 227. Basic Calculator II
#### Input
輸入一個字串，其中包含運算元及運算子，為了方便讀取，所有的運算子及運算元均以空格區隔。

運算元為 0 ~$2^{31}-1$ 的整數
運算子則包含 + - * / % 及 ( )

運算時請注意先乘除後加減及() 優先運算的計算規則
```=
3+2*2
(1+(4+5+2)-3)+(6+8)
3+5 / 2
```

#### Output
輸出結果。為了避免小數點誤差，所有的運算過程都不會產生小數點，可以放心使用整數進行運算
```=
7
23
5
```

#### Answer

```python=
import sys
for s in sys.stdin:
    print(eval(s.replace("/", "//")))

```

---


### 身分證檢驗
:::info
a020: 身分證檢驗
:::

#### Input
```=
1 2 1 3 4
```

#### Output
```=
2
```

#### Answer
main.py`

```python=

#Code

```

---


### 客服中心
:::info
a054: 電話客服中心

英文餘數	英文字串
0	"BNZ"
1	"AMW"
2	"KLY"
3	"JVX"
4	"HU"
5	"GT"
6	"FS"
7	"ER"
8	"DOQ"
9	"CIP"
:::

https://sites.google.com/site/zsgititit/home/c-cheng-shi-she-ji/a054-dian-hua-ke-fu-zhong-xin
#### Input
```=
130245675
123456789
```

#### Output
```=
FS
AMW
```

#### Answer
main.py`

```python=
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Created on Sun Apr  7 02:30:48 2019
@author: sam0225
"""

dict1 = {'A': 10, 'J': 18, 'S': 26, 'B': 11, 'K': 19, 'T': 27, 'C': 12, 'L': 20, 'U': 28,
         'D': 13, 'M': 21, 'V': 29, 'E': 14, 'N': 22, 'W': 32, 'F': 15, 'O': 35, 'X': 30,
         'Y': 31, 'G': 16, 'H': 17, 'Q': 24, 'Z': 33, 'I': 34, 'R': 25, 'P': 23}
while True:
    try:
        id_text = input()
        c = int(id_text[-1])
        id_text = id_text[:-1]
        total = 0
        n = 8
        s = ""
        for i in id_text:
            total += int(i) * n
            n -= 1

        for i in range(10, 36):
            if (i // 10 + i % 10 * 9 + c + total) % 10 == 0:
                s += list(dict1.keys())[list(dict1.values()).index(i)]
        print(''.join(sorted(s)))
    except:
        break
```
https://github.com/Sam-0225/Zerojudge-answer/blob/master/a054.py

```c=

#include <iostream>
#include <string> 
using namespace std;
int main()
{
	long long int a[26]={1,0,9,8,7,6,5,4,9,3,2,2,1,0,8,9,8,7,6,5,4,3,1,3,2,0};
	string c;
	while (cin >> c) {
		int d[9], num;
		for (int i=0;i<9;i++) {
			d[i]=c[i]-48;
		}
		num=d[0]*8+d[1]*7+d[2]*6+d[3]*5+d[4]*4+d[5]*3+d[6]*2+d[7]+d[8];
		num=(10-num%10)%10;
		for (int i=0;i<26;i++) {
			if (a[i]==num) cout << char (i+65);
		}
		cout << "\n";
	}
}
```
#https://zerojudge.tw/ShowThread?postid=18333&reply=0

### Zerojudge-answer
https://github.com/Sam-0225/Zerojudge-answer

---
### 出題模板
:::info
+ 什麼是質數?
    + 1.意義：一個數字如果除了１和自己之外，沒有其他正因數時，稱為質數。
    + 例如 2(＝１×２)，3、5、7、11…均為質數。而４、６、８不為質數。(因為最少還有因數2)

+ 質數的特性?
    + １質數除了２之外，必為奇數。（換句話說，２是最小的質數，也是唯一的偶數）
    + ２「１」不算是質數。
    + ３「算術基本定理」：比１大的任何整數，必可分解為質因數的乘積，且表法唯一。
:::

$a=\frac {c}  {b}$ 
$a= c \div b$
a稱為商數，b稱為除數，c稱為被除數。

如果除式的商數 $a$ 必須是整數，則稱為帶餘除法，$a \times b$ 與$c$ 相差的數值，稱為餘數 $d$。
$c \div b = a \dots d$
這也意味著
$c = a \times b + d$

:::info
運算思維結構訓練
訓練一：認識問題
訓練二：撰寫步驟
訓練三：歸納與整理
訓練四：繪製流程圖
訓練五：撰寫程式
授課大綱
在演練一，問題一：剪刀石頭布
在演練二，找1~1000質數 ，處理此題目的步驟 10/13 邏輯有錯
第8週
運算思維訓練五與實例演練一、二
第9週
運算思維訓練五與實例演練三、四


運算思維的關鍵步驟
1)分解問題：將一個大問題，拆解成許多的小問題
2)模式識別：尋找問題的相似度及前後關聯性，並予以歸類
3)抽象思維：化繁為簡，去掉多餘，專注核心
4)邏輯推演 (演算法)：以流程圖方式，描述解決問題的步驟和程序運

沒有運算思維，程式設計將顯得雜亂無章。
沒有程式設計，運算思維將會變得很空泛！
:::


#### Input
```=
1 2 1 3 4
```

#### Output
```=
2
```

#### Answer
main.py`

```python=

#Code

```

---

### 出題模板
:::info
說明 $x^2$
:::
#### Input
```=
1 2 1 3 4
```

#### Output
```=
2
```

#### Answer
main.py`

```python=

#Code

```

---

### Python - 100天从新手到大师
https://github.com/jackfrued/Python-100-Days

### All the good tutorials found for Competitive Programming
http://codeforces.com/blog/entry/57282

### How to prepare for ACM – ICPC?
https://www.geeksforgeeks.org/how-to-prepare-for-acm-icpc/
